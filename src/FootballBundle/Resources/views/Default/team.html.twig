{% extends 'FootballBundle::base.html.twig' %}

{% block body %}
    <h3>{{ name }}</h3>
    <div>
        <ul>
        {% for player in players %}
            <li><a href="" {{ player.name }}</li>
        {% endfor %}
        </ul>
    </div>
    <div>Coach: {{ coach.name }}</div>
{% endblock %}